# Backend for UIGrades

To run check the [Root ReadMe](../README.md)

This directory contains `app.js` which initializes the SQL database as well as routes to query the db.

All data is located in [./data](./data).